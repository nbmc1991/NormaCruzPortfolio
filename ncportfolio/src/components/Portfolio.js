import React, {useEffect, useState} from 'react';
import TinderCard from 'react-tinder-card';

<div>
    <TinderCard
    className='swipe'
    key={person.name}
    preventSwipe={['up','down']}>
        <div
         className="card" 
         style={{ backgroundImage: url(budgetbuddyapp.png)}}>
            <h3>{person.name}</h3>

        </div>
    </TinderCard>
    </div>

export default DevCards;